Brief Builder implementation specification — 18 September 2026

Implement against HTML hash `3a285fde0ac54ae9a469b30fdae8968be18e467a4bf05d5ea81e075eacf5188d`. This specifies a redesign, not changed pilot policy. S1–S23 identify the linked sources in [research.md](research.md), all accessed 2026-09-18. Proposed conditions remain proposals until approved.

Keep ten sheets, their order and every critical item below. Each face has a conclusion title, principal exhibit and implication; ledgers retain supporting records. Aim for 200–350 visible words normally, 550–700 on 09; hard limits remain 650/750, titles 24, each takeaway 20. Never shorten verbatim clippings. No external resources; tokens are usage; dollars appear only in 10’s API ledger footnote. Keep the budget in words, except original cited conflict positions and explicitly flagged draft errors.

Information architecture and regression mapping. F means default-visible face; L means embedded ledger, accessible offline. Titles below are proposed action titles.

| Sheet / title | F: required content | L: retained supporting content |
|---|---|---|
| 01 — Approve a conditional four-week pilot to test whether cited drafts reduce briefing effort. | Ask: live use in week 4 conditional on retrospective results. Tiles: 17/17 on one synthetic project; 0.98 M stage-one tokens; 33 minutes summed stage time versus 25.2-minute continuous capture; three conflicts, none machine-resolved. Plain token definition: units of text processed. Cost and workspace terms unresolved; sponsor owns conditions. Executive route 01→02→10. | 0.53 M Haiku 4.5, 0.45 M Sonnet 5; 56% cache reads, 27% cache writes; original source table, timing provenance and synthetic scope. |
| 02 — Nine steps prepare the work; a person authorizes the move into shadow creative. | Nine steps: six AI, two plain code, one person. Keep both stage-one flow rows, sign-off band and stage-two row. Static review pages; command-line operation. Account lead records resolutions/signature in brief object; creative lead evaluates; traffic owns pilot-stub spec table. Agency workspace, DPA, EU processing, zero retention are requirements awaiting terms confirmation; S0–S1 only. AI specialist operates; two champions trained by week 4. | Architecture, paths, routing history, schema fields and runbook deliverable; no fabricated command or existing runbook claim. |
| 03 — The readiness gate checks the input folder before AI classification. | Four inputs: RFP, transcript, email thread, guidelines, with existing clippings. Four non-AI checks: RFP present, ≥2 substantive sources, money mentioned, date mentioned. Show missing-RFP STOP separately. Sufficient-input classification: advertising creative, high confidence, S1 (second-lowest sensitivity tier). | File metadata, exact verdicts, classifier evidence/model usage and tier policy. |
| 04 — Thirty-three facts retain receipts; garbled terms stay flagged. | 33 cited facts; two garbles flagged, never repaired. Keep κι βίζουαλ clipping and fidelity note; budget receipt «είμαστε κάπου στα ογδόντα…» in its existing full wording. Transcript sent back once. | Existing speculative-dance receipt; both fidelity/extraction records. Separate later verifier table, explicitly “not in the graded totals,” preserving date, model, tokens and issue. |
| 05 — Three contradictions remain open until a person decides. | Audience, budget and launch-date pairs, both sides cited, all three resolution lines blank. Timeline labelled “two of four recorded positions.” | Seven candidate fields, synthesis mechanism, sources and X3 scope. |
| 06 — One master record supports both languages; Greek quality still needs review. | 25 cited entries, three conflicts, ten questions. Code recomputes 5/7 fields, 4% low confidence, ready for review. Non-empty EL/EN facing extracts, at least two sections each. No translation step: both renders use one object. Greek register is a pilot metric. | Full existing section inventory, example questions, nine protected terms, synthesis/render usage, semantic-support caveat. |
| 07 — A named account lead resolves three conflicts and signs the record. | Three red resolutions with CM initials; Christos Maragkoudakis, 2026-07-24. Budget remains in the eighties excluding media; combined total unknown. brief_review.html displays decisions; account lead records them in brief.json. | conflicts[n].resolution/resolved_by, signoff fields, schema validation, synthetic-role provenance and gate. Review was untimed; expose PRD time-definition mismatch. |
| 08 — Seventeen checks passed on one synthetic project; usefulness remains to be tested. | 17/17 scoped claim. T1:4, T2:6, T3+X:7 summary rows with identifiers visible on phones. Citation presence does not prove semantic support or natural Greek. | All 17 individual checks and results; X3 exactly “no invented or resolved total in the brief or either render”; sealed-key provenance. |
| 09 — Shadow creative needs three corrections and a creative lead’s judgement. | Draft A claude-sonnet-5 versus B claude-opus-4-8; A/B/neither question and blank creative-lead signature, owner/date unassigned. Proposition, hypothesis, condensed tone/feel/reasons-to-believe. Three corrections: unsupported Greek-made; existing quoted Draft A total inside mark.flag; erroneous “floated speculatively and retracted,” explicitly distinguished from metro retraction. Separate health-claim judgement. Six blockers: ingredients; video quantities/durations/formats; key-visual quantities/uses; TikTok-first scope; KPIs; milestones. Two-row spec table, traffic-owned pilot stub, token-match limitation. Preserve kv-art 1:1 and 9:16 MOCK-UP crops; packaging plate labelled generated for walkthrough, undefined brief context. SHADOW MODE slug: evaluation only, nothing delivered; pipeline emits text only. | Draft run records, claim citations, remaining/nonblocking questions, art rationale and crop caveat. Preserve original excerpts; no newly implied approval or creative-release date. |
| 10 — Approve the pilot conditionally; resolve operating cost and capacity before commitment. | Exactly three decisions with owners: sponsor approves conditional four-week pilot; operator re-baselines; management + operator confirms subscription capacity/terms after re-baseline. Compare conditional approval with deferral in decision 1. Targets: >80% question precision, >70% survival, <30-minute review target pending definition agreement; Greek 1–5 plus EL/EN edits; adoption 2/2 and ≥3 additional leads month 2. 0.98 M/1.13 M tokens, nine model calls; 598 743 versus 295 774. 210 hours/year worth €4.0–4.2k, gross capacity before unknown costs. Visible Limits: subscription window, not price; refusals 3/10; second fixture 15/17 then 16/17. Budget note «κάπου στα ογδόντα». | Formula (120 − 50) × 180 ÷ 60 × €19–20; A1 baseline, A2 volume, A4 rate, 50-minute target. Two leads × three projects, week-one baseline, weeks 2–3 retrospective, conditional week 4. Keep $3.55/$2.81 versus EVIDENCE.md $4.29 footnote and COST_MODEL.md §3 versus PRD reconciliation. Preserve usage/timing tables with separate units; explain nine calls as recorded stage-one agent attempts/sessions including repair, not necessarily nine API requests. |

Visual tokens (design choices informed by S6–S8):

| Token | Value / use |
|---|---|
| Colour | paper #F1F2EC; ink #1A1A18; pencil #5A5D55; red #C8102E only recorded human decisions. White #FFFFFF receipts; rule #C9CFC1 and band #E5E9DF decorative only. Essential borders use pencil. Blue #1B4F8A/yellow #F5C518 only inside 09 artwork; SVG flat blue/yellow/white, no opacity/gradients. |
| Fonts | Narrative: Charter, Cambria, Georgia, serif. Labels: system-ui, Arial, sans-serif. Receipts metadata: ui-monospace, Menlo, Consolas, monospace. No font downloads. |
| Scale | Title 36/42px desktop, 28/34px phone; section 22/28; body/quotes 18/28; notes 16/24; metadata 14/21; numerals 44/48. Implement in rem; prose ≤62ch, titles ≤38ch. |
| Spacing | 4, 8, 12, 16, 24, 32, 48, 64px; receipt padding 24px desktop/16px phone. |
| Grid | Max-width 1280px, 12 columns, 24px gaps, 48px outer padding. Full-width title; exhibit 9 columns, provenance 3. DOM order: title, implication, exhibit, provenance, ledger. No fixed sheet height. |
| Breakpoints | ≥1024px desktop; 640–1023px two-column exhibits where readable, padding 24px; <640px single column, padding 16px. Navigation reserves height plus safe-area inset. |

Remove decorative shadows and repeated role stamps; retain one compact labelled AI/code/human legend. Red never decorates asks, pending signatures, headings or navigation. Captions remain adjacent to quotes; glosses are labelled interpretation. Each claim has source/location; full paths stay in ledgers. Use captions, scoped headers, tabular numerals and U+2009 thousands separators outside verbatim material.

Interaction/accessibility (S10–S19): provide main landmark, skip link, labelled sheet regions, coherent subordinate headings, lang attributes and textual status labels. Keep h1 sheet titles for gate compatibility. Use ≥4.5:1 normal text, ≥3:1 large text/essential graphics; 44px controls, never below WCAG’s applicable 24px minimum. Ink focus outline 2px/3px offset; focused content stays above navigation. Support 200% text resizing and 320px reflow without page-wide horizontal scrolling. Only genuine data tables may scroll in named keyboard-focusable regions; never clip receipts or summary rows. Preserve reduced-motion CSS; remove decorative entrance/drawing animations.

Keep native details/summary, no nested disclosures. Add read-all and expand-ledgers controls. Use real #ch01–#ch10 links; accept legacy #01–#10. Initialize successfully before hiding sheets. Handle hashchange and popstate without duplicate history entries; direct ledger links reveal their sheet and disclosure. Back/Forward restores sheet/scroll; user navigation focuses title. Restrict C to focused navigation; ignore global shortcuts in interactive/editable contexts. Contents traps focus, makes background inert, closes with Escape and restores opener. Swipes exclude controls/scroll regions. No-JS mode shows all sheets with working anchors.

Inline everything; preserve one JPEG and shared SVG with dimensions/alt text. No fetch, imports, remote links/resources or required repository companions. Replace repo hyperlinks with source-path text; embed essential existing excerpts. Do not imply full unseen drafts are included. Maintain approximately current payload; investigate growth beyond 300 KB.

Print (S15): A4 portrait, 12mm margins; all sheets and ledgers, no fixed nav/overlay/sticky positioning/animation. Start each sheet on a new page, allow continuation pages; avoid splitting receipts, table rows or artwork, repeat table headers. Use 11pt body/9pt metadata; preserve status text in grayscale. Open disclosures before print, restore afterward; CSS must expose disclosure content with JavaScript disabled, verified in target browsers.

Exact gate contract: checks.py uses regexes, not a DOM. Preserve these lexical forms and all matching containers; do not exploit optional checks or change the gate. ROOT is `/Users/chrism/AI-transformation-assignment/brief-builder`; PAGE defaults to ROOT/WALKTHROUGH.html, overridden by `--file <path>`. Fixture reads remain under ROOT/fixtures/northlight_01 regardless of PAGE. A later implementer runs `python3 ../checks.py --file <candidate.html>` from this directory.

1. Quotes: `<blockquote…>…</blockquote>`, exact `<mark class="cited">`; text extraction removes exact `<span class="sic">sic</span>`, strips tags, decodes entities, NFC-normalizes. Fixture corpus concatenates transcript_kickoff.md, rfp_meltemi.md, emails_thread.md, background_brand_guidelines.md and removes `**`; blockquotes additionally normalize curly apostrophes/edge ellipses. Preserve per-source fidelity beyond this permissive test.
2. Dependencies: scans src/href for http(s), any `<link`, `@import`, `url(`. None allowed.
3. Sheets: `<section class="sheet…" id="chNN" data-n="N"` in that order, sequential 01–10/1–10. IDs t01–t10 required. Folios match `NN / NN &middot; Contents`; total equals count. `var CH = [` through newline plus two spaces plus `];`, counts literal `t: '` entries: ten.
4. IDs: every whitespace-prefixed `id="…"` unique. Every `href="#…"` and literal single-quoted `getElementById('…')` resolves. Existing literal targets: ruler, fol, back, next, next-title, announce, contents, contents-list, contents-close. Dynamic tNN and glyph/kv-art fragment references also remain valid.
5. Figures: when figcaption exists, nothing follows `</figcaption>` before figure close. Keep doctype. Bare `<script>…</script>` bodies undergo `node --check`.
6. Balance parser treats br/hr/img/meta/link/input/line/rect/circle/use/path as void; all other tags must balance. Avoid new self-closing SVG tags outside that list.
7. Budgets: exact `<h1 class="title action"…>` ≤24 whitespace words; `<ul class="takeaways">` with bare `<li>` ≤20 each. All titles obey cap, including cover. Sheet-body counting removes details blocks; ≤650, ch09 ≤750. Avoid nested sections/details that confuse regex boundaries.
8. Budget guard HTML-decodes after removing `<s>` and exact `<mark class="flag">` regions; rejects `€\s?8[0-5][,.]?\d{3}`, `\b8[0-5],000\b`, `\b8[0-5]k\b`. Preserve flagged quotation, never encode around guard.
9. Art: exact `<g id="kv-art">…</g>` immediately preceding `</defs>`; no “gradient”, opacity/stroke-opacity attributes; six-digit fill/stroke only #1B4F8A/#F5C518/#FFFFFF. Requires Meltemi Fizz. Wordmark/tagline regexes use text x="1280", integer y/font-size, attribute order y before font-size; circles use integer cx/cy/r. Clear-space calculation: cap=.72×font-size rounded; vertical zone baseline−cap−font-size to baseline+font-size, x=825…1735; circle radius gets +5. Tagline cap-top must clear zone.
10. MOCK-UP text regex expects integer x/y, text-anchor, font-size, letter-spacing in that order. Estimated width `26*(.62*fontSize+letterSpacing)` must fit x=740…1820; preserve current geometry. Literal SHADOW MODE required.
11. Exact `<ol class="decisions">` contains exactly three newline + eight spaces + `<li>` matches. CSS must contain `.decisions > li {`; nested lists do not increment counter.
12. Exact `<div class="page" lang="el">`/`en`, ending newline + eight spaces + `</div>`, each contains ≥2 literal `<h4>`. Nonempty class-prefix containers hero/brief/exam/numbers/decisions/limits/takeaways/flow on div/ul/ol/article must yield ≥3 words under the checker’s first-close regex.

The gate does not establish accessibility, full regression coverage, packaging presence or factual entailment. Preserve those through review. Its script check writes temporary JS; this research task did not execute it.

Numbered deviation register; all changes above fall under these bounded changes:

1. Shorten faces/titles, collapse repeated explanation and relocate architecture: one governing point and reading hierarchy, [Duarte/S2](https://www.duarte.com/blog/the-art-of-document-design-tips-and-tricks/), S1/S3/S6.
2. Add executive route, conditional/defer comparison and visible unknown-cost ownership; reconcile time definitions without inventing approvals: decision completeness, [AICD/S4](https://www.aicd.com.au/content/dam/aicd/pdf/tools-resources/director-resources/board%20papers_may%202020_final_%20appendix_an%20annotated%20sample%20board%20paper.pdf), S5/S20.
3. Separate stop/classify paths, later verifier and units; clarify attempt counts and check scope: calibrated evidence, [NIST/S20](https://airc.nist.gov/airmf-resources/airmf/5-sec-core/), S8/S21.
4. Reorder 09 around A/B, corrections and blockers; retain condensed creative substance, labels and artwork: prototype/provenance clarity, [Design Council/S22](https://www.designcouncil.org.uk/resources/framework-for-innovation/), S23.
5. Apply tokens, grid, spacing and simpler decoration; remove outside-art swatches, pending red and stale art description: hierarchy/semantic consistency, [NN/g/S6](https://www.nngroup.com/articles/good-visual-design/), S7–S9 plus fixed palette rules.
6. Add landmarks, skip link, larger controls, visible mobile tier IDs and safe shortcut scope: accessibility, [WCAG/S10](https://www.w3.org/TR/WCAG22/), S11–S13.
7. Add read-all/expand controls, real fragments, failure-safe initialization and offline source handling: progressive enhancement/history, [WHATWG/S14](https://html.spec.whatwg.org/multipage/interactive-elements.html#the-details-element), S16–S19.
8. Strengthen print pagination and no-script disclosure visibility: complete printable evidence, [MDN/S15](https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Media_queries/Printing).

Acceptance: deterministic gate green; every F/L item present; clippings unchanged; no network requests opening from disk; keyboard/modal/history/no-JS paths work. Inspect 320/375/768/1280px, text zoom, reduced motion and A4 print. Repeat CMO benefit/conditions, CFO arithmetic/unknowns, creative evidence-versus-hypothesis and engineer operating-boundary tasks on one frozen candidate. No visual score overrides a missing critical item.
