You are the design lead at a small studio. The client approved your refined lockup tools/walkthrough/logo/dir1_final.svg ("Meltemi Hand", preview dir1_final_sq.svg.png) but wants the alternative the design lead suggested: the two white bubbles should be RELEASED FROM the second crest, part of the same gesture, not floating above it. Produce ONE alternative file, tools/walkthrough/logo/dir1_final_alt.svg, that changes only the bubbles (and, if needed to make the release read, the last few units of the stroke's second crest), keeping everything else byte-identical: frame, rect, the three text elements, the stroke path's overall geometry, colours.

Design intent: the wind stroke crests and the fizz leaves it. The largest bubble should sit ON the outgoing shoulder of the second crest (overlapping or tangent to the yellow stroke, so it reads as breaking off), the smaller one a short distance further along the same trajectory, up and to the right; radii decreasing (keep 62 and 34, or 62/36/22 if a third tiny bubble makes the release clearer, no more than three). White bubbles over the yellow stroke are fine (white on yellow on blue, all flat). Keep the mark inside x 740–1820 and y 200–1440; nothing but the wordmark inside y 1440–1772 across x 825–1735; exactly the three colours; no opacity, gradients, filters; under 12 KB; the three text elements verbatim (use the literal middle dot, never &middot;).

Render for comparison, then look with view_image: square crop (wrap the inner content in <svg viewBox="320 0 1920 1920">) saved as dir1_final_alt_sq.svg and rendered with `qlmanage -t -s 1024 -o tools/walkthrough/logo tools/walkthrough/logo/dir1_final_alt_sq.svg`; a 128 px and a 32 px render into tools/walkthrough/logo/small/. At most three attempts; the release must read at 1024 px and the largest bubble must still be visible at 32 px. Write tools/walkthrough/logo/alt_notes.md: what moved (coordinates before/after), why, and the validation output (XML parse, palette, prohibited features, text elements verbatim, clear-space zone, crop bounds, size). Do not edit WALKTHROUGH.html or any other existing file; no git commands that change state. Finish with a short report: file, attempts, anything still imperfect.
