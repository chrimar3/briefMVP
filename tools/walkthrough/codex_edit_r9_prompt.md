You are the editor for WALKTHROUGH.html in this repository (a single self-contained HTML presentation). Round 9 of the review loop produced an editor brief of twelve bounded, text-only items: tools/walkthrough/codex_round/r9/brief_items.md (the full evidence is in tools/walkthrough/codex_round/r9/result.json). Apply them to WALKTHROUGH.html, in order, with this discipline:
- Before applying an item that asserts a fact about the pipeline (items 1, 2, 5, 11, 12), verify it against the code or document it concerns (CLAUDE.md architecture section; pipeline/creative.py spec-match gate; schema/brief_schema.json; pipeline/review.py; docs/PRD.md section 8; docs/demo_timing.md). If the brief's wording is not what the code does, write the wording the code supports and say so in the report. If an item cannot be verified, skip it and say why.
- Items 6 to 9 reframe sheet 10's decisions as proposed conditions. Keep exactly three top-level decisions with owners; keep the tokens-not-dollars rule; never invent a figure (no pilot budget, no fee, no threshold that is not in docs/PRD.md or docs/pilot/SCORECARD.md; the scorecard's thresholds may be cited as "proposed, docs/pilot/SCORECARD.md").
- Never change the text inside any <blockquote> or <mark class="cited">. Do not add external resources. Keep action titles at most 24 words and takeaways at most 20; keep every sheet at most 650 visible words (750 on sheet 09): if an item would exceed a budget, shorten the item, not other content.
After every batch run: python3 tools/walkthrough/build.py && python3 tools/walkthrough/checks.py ; keep editing until checks.py exits 0, and do not finish otherwise. Do not touch any other file; no git commands that change state. Finish with a report (this final message): per item, applied / adapted (with the wording used and the evidence) / skipped (why); the final checks.py output; visible words per sheet.
