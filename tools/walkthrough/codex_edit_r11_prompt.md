You are the editor for two pages in this repository: WALKTHROUGH.html (v1, root) and tools/walkthrough/redesign/WALKTHROUGH_v2.html (v2, generated by tools/walkthrough/redesign/build_v2.py from tools/walkthrough/redesign/source_snapshot.html plus v2.css and v2.js in that folder). Round 11 verified three genuine hard failures; apply exactly these, nothing else (tools/walkthrough/codex_round/r11/result.json has the evidence; its "missing_exhibit" items about review scope, constraints and acceptance conditions are NOT page defects and must NOT be added to any page):
1. Sign-off ownership (both pages). Verify first in .claude/agents/synthesize.md (the synthesize agent writes signoff.status = "draft"), pipeline/stages.py (validation rejects any other status) and pipeline/creative.py (the creative stage requires signed_off). Then replace every claim that a model cannot write the sign-off field. In v1: sheet 07 marginalia "The machine cannot sign; the sign-off field is not even writable by a model." → "The machine cannot sign: synthesis writes signoff.status as draft and validation rejects any other value; only a person records signed_off."; sheet 07 ledger foot "A model never writes that field." → "A model can only write draft; signed_off is recorded by a person."; sheet 02 gate band "a model cannot write this field" → "a model can only leave it as draft"; sheet 02 ledger "Nothing passes the sign-off gate without a person; a model cannot write that field." → "Nothing passes the sign-off gate without a person; a model can only leave the field as draft." Keep every sentence within its budget.
2. Swipe (both pages): add a touchmove listener that cancels the gesture when vertical movement exceeds 40 px or a second touch appears, and clear the gesture in the scroll listener, so a scroll can never fire Next. v1: the script block in WALKTHROUGH.html (touchstart/touchend near the end); v2: tools/walkthrough/redesign/v2.js.
3. v2 only: the last sheet's Next button must have an accessible name that contains its visible label. In v2.js paint(): set the visible span to "Start again" on sheet 10 and the aria-label to "Start again: return to sheet 01" (WCAG 2.5.3 label in name); on other sheets keep "Next" and "Next sheet: <title>".
Then: cp WALKTHROUGH.html tools/walkthrough/redesign/source_snapshot.html; cd tools/walkthrough/redesign && python3 build_v2.py; cd back to the repository root; run python3 tools/walkthrough/build.py && python3 tools/walkthrough/checks.py && python3 tools/walkthrough/checks.py --file tools/walkthrough/redesign/WALKTHROUGH_v2.html; both must exit 0; iterate until they do without weakening any rule. Confirm with grep that no page still contains "not even writable", "never writes that field", "cannot write this field" or "cannot write that field". Never change text inside <blockquote> or <mark class="cited">; no external resources; do not touch any other file; no git commands that change state. Report: per item, what changed in each page (before and after), the two checks outputs, the grep result.
