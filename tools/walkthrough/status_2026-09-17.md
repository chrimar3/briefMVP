# Project state, 2026-09-17 evening (for a leverage assessment)

Read with HANDOFF_WALKTHROUGH.md (root), tools/walkthrough/codex_review.md (your earlier review of the walkthrough and the method), docs/PRD.md §7–9, docs/COST_MODEL.md, docs/EVIDENCE.md, runs/voreas_prep_report.md, CLAUDE.md.

## Done since your review
- Your 42 open items applied (codex_fix.md); gate `tools/walkthrough/checks.py` green; artifact republished.
- Method changed as you recommended: `score-round-v3.js` = hash-frozen candidate, fidelity + rendered-UX auditors listing hard failures with evidence, two skeptics per claim, four audience tasks (CMO explains, CFO reproduces economics, creative director separates evidence from hypothesis, engineer states the operating boundary), the three original personas for the time series, stability verdict (no hard failures + tasks pass + no aspect down ≥ 0.5), bounded editor brief.
- Round 7 ran partially (judges and brief died on a session limit). One hard failure confirmed in headless Chrome (sheet-08 summary rows overlapping ≤ 560 px) and fixed with 25 other precision/UX edits (`edit_r7.py`). Audience tasks all "passed" but listed gaps (below).
- Packaging: the owner asked for a can; Codex generated two (A = original lockup, B = bottle label); can A is embedded on sheet 09 "for now". The owner judged the original lockup "generic, not ownable"; Codex produced three wind-territory directions; the owner chose "Meltemi Hand" and asked for it to be adjusted to the brand voice and client data; that refinement run is in flight (`logo/dir1_final.svg`).

## Pending, walkthrough
- Finish the logo: swap `dir1_final.svg` into the sheet-09 SVG (`swap_kv.py`), re-render the can from it, embed, gate, republish; then round 8 under v3; stop on two consecutive stable reviews.
- Audience-task gaps the page cannot fill from the repository (the page says "still to be estimated"): the euro cost of running the pilot (operator/engineering hours, training, subscription plan and its window); the runbook path (PRD §8 names a one-page runbook, none exists in the repo); the `brief.json` field that holds a resolution's rationale; dated model ids for claude-sonnet-5 / claude-opus-4-8 (the manifest recorded aliases); one review-time target (sheet 07: under 30 min review, PRD §7; sheet 10: 50 min attention = 20 assembling + 30 reviewing, PRD §2).
- Nothing from the walkthrough work is committed (WALKTHROUGH.html, tools/walkthrough/, HANDOFF_WALKTHROUGH.md are untracked): owner's call.

## Pending, repository (found, not edited)
- docs/EVIDENCE.md says the tier3 manifest totals $4.29 (raw double count of the creative arms); de-duplicated $3.55.
- docs/COST_MODEL.md §3 "~€5–6k/yr returned" does not reconcile with PRD A1/A2/A4 + §2's 50-min target (≈ 210 h ≈ €4.0–4.2k).
- config/channel_specs.json is a self-declared stub; the creative brief presents its rows as fixed specs.
- runs/tier3/creative/creative_brief_opus.md calls the TikTok dance "retracted" (it was speculative; the metro idea was the retraction).
- pipeline/review.py makes a review view, not an approval editor; resolution write-back is "v2".
- All published timing/usage numbers (README, EVIDENCE.md, demo_timing.md) are the Haiku-era baseline; extraction moved to Sonnet + a verifier on 30 July (transcript leg measured 598 743 vs 295 774 tokens); no whole-brief re-baseline exists.
- C4 (PRD DR-1 production shape: API transport) never measured; needs a key.
- Second fixture (voreas) audit findings queued for the owner: garble-flagged claim next to a clean clause gets dropped by synthesis (client's primary objective, 2/2 rolls); questions duplicating open conflicts; renders with uncited lines that no check sees; currency-gate blind spot for unmarked spoken numbers.

## Decision context
- PRD §8 pilot: two account leads, three past projects each, weeks 2–3 retrospective, week 4 live, creative in shadow, S0–S1 clients; targets §7: question precision > 80 %, draft survival > 70 %, review < 30 min, Greek register, voluntary adoption.
- The client will most likely run on a subscription (usage window, not price per brief); the PRD's API-terms assumption is superseded and unresolved.
