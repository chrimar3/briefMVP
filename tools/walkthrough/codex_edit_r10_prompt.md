You are the editor for WALKTHROUGH.html in this repository (a single self-contained HTML presentation). Round 10 of the review loop verified two hard failures and two precision items; apply ONLY these four, from tools/walkthrough/codex_round/r10/brief_items_facts.md (evidence in tools/walkthrough/codex_round/r10/result.json). The round's other eight items (layout, disclosure and cuts) are deliberately NOT applied to this page: a redesigned page is being built separately under the design standards. Discipline:
- Item 1 (readiness): the runner computes the readiness block from the brief with config/readiness_policy.json (pipeline/runner.py step 6; skills/SYNTHESIS.md rule 8 forbids the model from writing it); exam check T2.6 compares the stored block with the harness's independent recomputation. Fix every sentence on the page that says the model wrote it or that code "matches the model's" (at least the sheet-06 marginalia/ledger sentence containing "must match what the model wrote (exam check T2.6)" and the sheet-08 exam row "The readiness verdict, recomputed by code, matches the model's"); keep the T2.6 results text.
- Item 2 (answer key): the key is reserved for the grader by policy (pipeline/gates.py HARNESS_ONLY_FILES; every agent prompt says it is off limits; it is excluded from source discovery), not physically unopenable. Fix "a sealed answer key the pipeline cannot open" (sheet 03) and "it is the only code that can open the sealed answer key" (sheet 08 takeaway) to say policy and exclusion, keeping each within its word budget (takeaways at most 20 words); "the only code allowed to read" and "may open" wordings elsewhere are fine.
- Items 3 and 4 (sheet 09 precision): apply the brief's wording for "every fact cites a brief entry" (references that still require verification) and for the clear-space callout (the artwork is the wind stroke, two bubbles and the name; one wordmark height of clear space is required by the guidelines; the crops keep it, packaging compliance is unverified). Keep the citation spans.
- Never remove or shorten anything on this regression list: tools/walkthrough/round10_args.json "mustKeep". Never change text inside <blockquote> or <mark class="cited">. No external resources. Titles at most 24 words, takeaways at most 20, sheets at most 650 visible words (750 on sheet 09).
After editing run: python3 tools/walkthrough/build.py --out /tmp/walkthrough-r10-artifact.html && python3 tools/walkthrough/checks.py ; keep editing until checks.py exits 0. Touch no other file; no git commands that change state. Report: per item what changed (before and after), the final checks.py output, visible words per sheet.
